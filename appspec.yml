version: 0.0
os: linux
files:
- source: /index.html
  destination: /var/www/html/

hooks:
 BeforeInstall:
    - location: script/apache2_install.sh
      timeout: 300
      runas: root

 ApplicationStop:
    - location: script/apache2_stop.sh
      timeout: 300
      runas: root
